import { View, Text, StyleSheet } from 'react-native';

export default function AvailableMeals() {
    return (
        <View>
            <Text>The AvailableMeals component</Text>
        </View>
    );
}

const styles = StyleSheet.create({});
